# Dev Session Log — 2026-03-15

## What was done this session

### ✅ Notion Export Feature (merged to master)
- Backend: `make_notion_card()` in `main.py` + `api.py` returns `notion` field
- Frontend: `NotionCardPreview.tsx` component, Obsidian/Notion toggle in output panel
- Smart copy/download respects active output mode

### ✅ Code Quality
- ESLint: 0 errors
- deno lint: 0 errors (added `deno.json` to exclude `no-sloppy-imports` — Node.js project)
- Fixed deno lint `--fix` regression (added `.tsx/.ts` to imports which Next.js forbids)

### ✅ Vercel Deployment
- `master` → production at [obsiditube.vercel.app](https://obsiditube.vercel.app)
- `feature/paywall` branch created (no code yet)
- `vercel.json` added at repo root (framework config; `rootDirectory=frontend` set in dashboard)

### ✅ UI Improvements
- Auto Card Link plugin hint moved inside panel header (centered, more visible)
- README.md updated: deployed section, architecture, Notion export docs

### 🔲 Paywall Feature (planned, not implemented)
- Branch: `feature/paywall`
- See: `Docs/paywall-implementation-plan.md`
- See: `Docs/services-setup-guide.md`

## Pending (next session)

1. **User to complete service setup** (see `Docs/services-setup-guide.md`):
   - [ ] Create LemonSqueezy account + product + webhook
   - [ ] Create Resend account + verify domain
   - [ ] Create NOWPayments account + add wallets
   - [ ] Create Upstash KV via Vercel dashboard
   - [ ] Add all env vars to Vercel project settings

2. **Implementation** (once services are set up):
   - Backend: `/api/license/validate`, webhook endpoints, convert truncation
   - Frontend: `PaywallModal.tsx`, `ProBanner.tsx`, `Footer.tsx`, legal pages
   - GiBlok footer on all pages

## Branch State
- `master`: clean, deployed to production
- `feature/paywall`: branch created, `src/license.py` skeleton exists (to be replaced)
- `feature/notion-export`: merged into master, can be deleted
